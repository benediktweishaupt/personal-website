---
interface Props {
  label?: string;
}

const { label } = Astro.props;
---

<aside class="border-l-2 border-gray-300 pl-4 md:pl-6 text-base md:text-xl max-w-3xl space-y-4 text-gray-500">
  {label && <p class="text-xs uppercase tracking-wide text-gray-400">{label}</p>}
  <slot />
</aside>
